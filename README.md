# StreamFlix — Frontend Client

Netflix clone build with react and react-router

---

## 🎯 Overview

This is a **standalone SPA (Single Page Application)** that connects to the Express backend that uses the movie db api. The frontend is completely decoupled from the server.

### Tech Stack

- **React 19** - UI framework
- **React Router v7** - File-based routing in framework mode (`ssr: false` for SPA)
- **shadcn/ui** - UI component library (Radix UI + Tailwind CSS)
- **Tailwind CSS v4** - Styling with CSS variables
- **TypeScript** - Type safety
- **Vite**
